# if / elif / else

Beginner | python-core | control-flow

Implement a function using a full `if` / `elif` / `else` chain to classify a numeric input, establishing exactly how Python evaluates conditional branches.
